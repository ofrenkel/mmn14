#ifndef UTILS_H
#define UTILS_H

/*
 * Trim (strip whitespaces) the given string in place, returning a pointer to
 * the start of the trimmed string.
 *
 * Parameter str: The string to trim.
 * Returns: A ponter to the start of the trimmed string.
 */
char *trim(char *str);

#endif /* UTILS_H */